{
  "$schema": "https://turborepo.com/schema.json",
  "globalDependencies": [".env"],
  "globalPassThroughEnv": ["TURBO_API_URL", "TURBO_TOKEN", "TURBO_TEAM"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"]
    },
    "frontend#build": {
      "dependsOn": ["type-check", "lint"],
      "outputs": [".output/**"],
      "inputs": ["$TURBO_DEFAULT$", ".env"],
      "env": ["TURBO_API_URL", "TURBO_TOKEN", "TURBO_TEAM"]
    },
    "api#build": {
      "dependsOn": ["type-check", "lint"],
      "outputs": ["dist/**"],
      "inputs": ["$TURBO_DEFAULT$", ".env"],
      "env": ["TURBO_API_URL", "TURBO_TOKEN", "TURBO_TEAM"]
    },
    "lint": {},
    "type-check": {},
    "dev": {
      "persistent": true,
      "cache": false,
      "interactive": true
    }
  },
  "ui": "tui"
}
